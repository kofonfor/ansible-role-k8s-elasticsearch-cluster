kind: PersistentVolume
apiVersion: v1
metadata:
  name: elasticsearch-data-4
  labels:
    type: local
spec:
  storageClassName: elasticsearch
  capacity:
    storage: 50Gi
  accessModes:
    - ReadWriteOnce
  hostPath:
    path: "/data/pvs/elasticsearch-cluster-pv-4"
